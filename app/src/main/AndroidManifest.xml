<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.bhatnagar.arpit.wallet">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".Util.QRScanner"/>
        <activity android:name=".UI.MyPhoneNumber">
        </activity>
        <activity android:name=".UI.GetAmount">
        </activity>
        <activity android:name=".UI.TransactionComplete">
        </activity>
        <activity android:name=".UI.Otp">
        </activity>
        <activity android:name=".UI.OfflineQR">
        </activity>
        <activity android:name=".UI.OfflineQrResponse">
        </activity>
        <activity android:name=".UI.ReceivePayment">
        </activity>
        <activity android:name=".UI.AddAmount">
        </activity>
        <activity android:name=".UI.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".UI.SetNum">
        </activity>

        <activity android:name="com.journeyapps.barcodescanner.CaptureActivity"
                  android:screenOrientation="portrait"
                  tools:replace="screenOrientation"/>

        <receiver android:name=".Util.Network.NetworkChanged"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>